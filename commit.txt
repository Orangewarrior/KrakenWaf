 Implemented and validated release 2.44.0.

  - Redis GCRA now uses atomic Lua, TAT and Redis TIME: KrakenWAF/src/waf/rate_limit.rs:622
  - Typed decisions with accurate Retry-After.
  - Proxy split into seven contextual modules with Builder pattern: KrakenWAF/src/proxy/builder.rs:9
  - Fixed sharding, eviction, persistence, effective-IP consistency, configuration validation and HTTP 429 enforcement.
  - Updated English documentation: KrakenWAF/docs/rate_limit.md:352
  - Version and release notes: KrakenWAF/CHANGELOG.md:2

  Validation:

  - 419 unit tests passed.
  - 12 real rate-limit integration tests passed.
  - Clippy passed with -D warnings.
  - Real demo at concurrency 50: 677 blocked, 0 bypasses, 0 errors.
  - Redis daemon/TLS tests remain environment-gated because their local fixtures are unavailable.
